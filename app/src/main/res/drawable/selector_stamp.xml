<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 선택되지 않은 상태 -->
    <item android:drawable="@drawable/ic_stamp_variant" android:tint="?attr/colorOnSurfaceVariant" android:state_checked="false" />
    <!-- 선택된 상태 -->
    <item android:drawable="@drawable/ic_stamp_selected" android:tint="?attr/colorPrimary" android:state_checked="true" />
</selector>